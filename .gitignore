# Secrets — never commit
.env
*.key
*.pem
*.pub
ssh-key*
deploy_*.sh

# Claude local config
.claude/launch.json
.claude/scheduled_tasks.lock

# Preview files
dashboard/light_preview.html

# Logs and reports
logs/
*.log
backtest_report.json
forward_review.json

# Python cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# market_data.db and ensemble.pkl ARE committed so Railway has seed data on first deploy.
# After first deploy, Railway volume takes over for the DB.
# (Remove the lines below if you don't want to commit them — Railway will fetch fresh data on startup)
